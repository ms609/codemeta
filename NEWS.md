# codemeta (development version)

* `softwareRequirements` is now written as a JSON array; previously the
  `SystemRequirements` field (or `null`, when absent) turned it into an object.

# codemeta 0.1.1

* Fix handling of multiple URLs [#1]
* Added a `NEWS.md` file to track changes to the package.
